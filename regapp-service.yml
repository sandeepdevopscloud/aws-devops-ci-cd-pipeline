apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-app-lb
spec:
  type: LoadBalancer
  selector:
    app: my-app-lb
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
